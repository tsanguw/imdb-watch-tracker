.env
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
*.egg-info/

# Personal watchlist data never belongs in this (public) repo — it lives in
# a separate private repo instead. Kept here too as a defense-in-depth
# backstop in case something ever defaults to writing into data/.
data/imdb_watchlist_export.csv
data/watchlist.xlsx
/data-repo/
